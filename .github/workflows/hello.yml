name: hello-selfhosted
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['.github/workflows/hello.yml']
jobs:
  hi:
    runs-on: [self-hosted, Linux, X64]
    steps:
      - run: echo "hello from $(hostname) on $(date)"
